import { TSalesAdResponse, TSalesAdUpdate } from "../../interfaces/salesAd.interface";

export const updateById = async (salesAdId: string, salesAdData: TSalesAdUpdate): Promise<TSalesAdResponse | void> => {
    
};